import 'package:flutter/material.dart';

class UnlockAccountView extends StatelessWidget {
  const UnlockAccountView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Column(
        children: [],
      ),
    );
  }
}
